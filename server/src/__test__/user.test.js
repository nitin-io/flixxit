import supertest from "supertest";

describe("user", () => {
  describe("login user", () => {
    describe("given the correct username and password", () => {
      it("should return 200 ok", () => {
        expect(true).toBe(true);
      });
    });
  });
});
